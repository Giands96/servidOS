spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:5433/servidos_test}
    username: ${TEST_DB_USERNAME:servidos_test}
    password: ${TEST_DB_PASSWORD:servidos_test}
  jpa:
    show-sql: false

debug: false
